/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['img.freepik.com'], // substitua pelo domínio real da imagem
  },
}

module.exports = nextConfig
